# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- main

pool:
  vmImage: ubuntu-latest

steps:
- task: Docker@2
  displayName: Build and push an image to container registry
  inputs:
    command: buildAndPush
    repository: azure-alpine-test
    containerRegistry: jvazurecontainerregistry
    tags: |
      latest
- script: |
    docker run -e ANCHORE_CLI_URL=$ENGINEURL -t anchore/engine-cli anchore-cli image add docker.io/busybox:latest
    docker run -e ANCHORE_CLI_URL=$ENGINEURL -t anchore/engine-cli anchore-cli image wait docker.io/busybox:latest
    docker run -e ANCHORE_CLI_URL=$ENGINEURL -t anchore/engine-cli anchore-cli evaluate check docker.io/busybox:latest
  displayName: 'scan image with anchore'
  env:
    ANCHOREURL: $(anchore-engine-url)